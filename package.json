{
  "name": "@magloft/devkit",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "MagLoft DevKit",
  "author": "Tobias Strebitzer <tobias.strebitzer@magloft.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/magloft/devkit.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "clean": "lerna run build",
    "build": "lerna run build",
    "prepack": "lerna run clean && lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
